language: node_js

node_js:
  - "8"

cache:
  directories:
    - node_modules

deploy:
  provider: pages
  local-dir: dist
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  verbose: true
  on:
    branch: dev
